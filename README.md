# create-task
function start(){
    
}
function backGround(){
    
}
function rectangles(width,height,x,y,color){
    var rect = new Rectangle(width,height);
    rect.setPosition(x,y);
    rect.setColor(color);
    add(rect)
}
